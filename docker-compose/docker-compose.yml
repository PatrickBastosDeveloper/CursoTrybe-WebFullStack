version: '3.8'

services:

  mysql:
    image: mysql:5.7
    restart: unless-stopped
    environment:
      - MYSQL_ROOT_PASSWORD=root
    ports:
      - 3306:3306
    volumes:
      - ./data/MySQL:/var/lib/mysql
    network_mode: host
